export const useStyles = () => {
    return {
        text: {
            // color:  (theme) => theme.palette.primary.main,
            color: 'primary.main',
            backgroundColor: 'secondary.main',
            my: 5
        }
    }
}
